{
  "name": "dev-workspace-suite",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "prisma": "^5.22.0"
  }
}